mapscripts Tenderloin_Library_MapScripts {
    MAP_SCRIPT_ON_TRANSITION {
    }
}
script Tenderloin_Library_InteractCraig_Event_Script {
    lock
        switch(var(VAR_MASK_OFF_STATE)){
            case 0: return
            case 1: return
            case 2: return
            case 3: return
            case 8: return
            default: call(MaskOff_QuestG_Dialogue)
        }
    release
        end
}
